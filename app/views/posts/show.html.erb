<h1> <%= @post.title %> </h1>
  <%= link_to "Edit", edit_post_path(@post), class: 'btn btn-success' %>
<div class="media">
  <div class="media-body">
    <h4 class="media-heading">
      <%= @post.body %> </p> 
      </h4> 
  </div>
</div> 
