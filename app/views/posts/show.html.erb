<h1> <%= @post.markdown_title %> </h1>
  
<div class="row">
  <div class="col-md-8">
    <% if policy(@post).edit? %>
      <%= link_to "Edit Post", edit_post_path, class: 'btn btn-success' %>
    <% end %> 
  </div>
  <div class="col-md-8">
    <small>
        <%= image_tag(@post.user.avatar.tiny.url) if @post.user.avatar? %>
        submitted <%= time_ago_in_words(@post.created_at) %> ago by
        <%= @post.user.name %>
    </small>
    <h4><%= @post.markdown_body %></h4>
  </div> 
    <div class="col-md-8"> 
      <%= image_tag(@post.image.post_view.url) if @post.image? %>
    </div>
  <div class="col-md-8">
   <%= render partial: 'comments/form.html.erb', locals: { :topic => @topic, :post => @post, :comment => @comment } %>
  </div>
  <div class="col-md-8">
      <% @post.comments.each do |comment| %>
        <%= render comment %>
      <% end %>
  </div> 
</div> 
